<script lang="ts">
    import type { Property, Rarity } from "$lib/types";

    let {
        properties,
        score,
        rarity
    }: {
        properties: Property[],
        score: number,
        rarity: Rarity
    } = $props();


</script>